// Re-export of the auto-generated Supabase client so consumers can import
// from "@/lib/supabaseClient" if preferred. The canonical client lives in
// src/integrations/supabase/client.ts and is configured with the project's
// VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY (anon key).
export { supabase } from "@/integrations/supabase/client";
